## 1. Bootstrap the repository

- [x] 1.1 Initialize `go.mod` for `github.com/Miss-you/go-symphony`.
- [x] 1.2 Add `cmd/symphony` with a minimal entrypoint and a test-first red-green cycle.
- [x] 1.3 Add the approved `internal/...` placeholder package layout.

## 2. Verify the canonical commands

- [x] 2.1 Run `go test ./...`.
- [x] 2.2 Run `make build`, `make lint`, `make test`, `make test-e2e`, and `make verify`.
- [x] 2.3 Update the task board and workspace artifacts from verification evidence.
